# AI Domains

Domain registrations will define available tools, retrieval configuration, model
configuration, citation rules, and fallback behavior. Phase 1 creates only the
directory and seed domain records; it does not implement orchestration.
